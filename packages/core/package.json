{
  "name": "@oneclaw/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core runtime and domain modules for OneClaw",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.ts\""
  }
}
